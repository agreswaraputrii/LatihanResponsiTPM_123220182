name: restaurant_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.2.1 # Untuk melakukan panggilan HTTP/API
  shared_preferences: ^2.2.3 # Untuk penyimpanan data lokal (login, favorit)
  cached_network_image: ^3.3.1 # Untuk memuat gambar dari network dengan cache

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true